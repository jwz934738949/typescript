/** @format */
(() => {
    function initFun(params) {
        return "首次编写ts代码:" + params;
    }
    initFun("04-20");
    let age = 25;
    let name = "测试名称";
    console.log("name:" + name + ",age:" + age);
})();
